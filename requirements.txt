# API
fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2

# Inferencia YOLO
# ultralytics maneja YOLOv5, v8 y v11 automáticamente — detecta versión del .pt
ultralytics==8.3.40

# CPU-only PyTorch (más liviano y barato para Railway)
# Si más adelante quieres GPU, se cambia por torch normal
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.5.1
torchvision==0.20.1

# Procesamiento de imágenes
Pillow==11.0.0
numpy==1.26.4

# OpenCV headless (sin GUI, mucho más liviano que opencv-python)
opencv-python-headless==4.10.0.84
